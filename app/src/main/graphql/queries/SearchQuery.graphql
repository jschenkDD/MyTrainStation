query SearchQuery($searchTerm: String!) {
    search(searchTerm: $searchTerm) {
        stations {
            primaryEvaId
            name
            hasWiFi
            hasParking
            hasSteplessAccess
            picture {
                url
            }
        }
    }
}